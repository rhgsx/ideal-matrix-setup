---
all:
  children:
    matrix:
      children:
        haproxy:
          hosts:
            haproxy01:
              ansible_host: 10.0.1.10
        synapse:
          hosts:
            synapse01:
              ansible_host: 10.0.1.20
        database:
          hosts:
            postgres01:
              ansible_host: 10.0.1.30
        redis_cluster:
          hosts:
            redis01:
              ansible_host: 10.0.1.40
        livekit:
          hosts:
            livekit01:
              ansible_host: 10.0.1.50
        coturn:
          hosts:
            coturn01:
              ansible_host: 10.0.1.60
        workers:
          hosts:
            workers01:
              ansible_host: 10.0.1.70
        element:
          hosts:
            element01:
              ansible_host: 10.0.1.80
  vars:
    ansible_user: ansible
    ansible_ssh_private_key_file: ~/.ssh/matrix_deploy
    ansible_python_interpreter: /usr/bin/python3
