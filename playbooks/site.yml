---
# Главный плейбук — разворачивает весь стек в правильном порядке

- name: "01 · Common baseline (все хосты)"
  import_playbook: 01_common.yml

- name: "02 · Docker CE (все хосты кроме HAProxy)"
  import_playbook: 02_docker.yml

- name: "03 · PostgreSQL (Docker)"
  import_playbook: 03_postgres.yml

- name: "04 · Redis (Docker)"
  import_playbook: 04_redis.yml

- name: "05 · Coturn (Docker, host network)"
  import_playbook: 05_coturn.yml

- name: "06 · LiveKit SFU + JWT (Docker, host network)"
  import_playbook: 06_livekit.yml

- name: "07 · Synapse main (Docker, host network)"
  import_playbook: 07_synapse.yml

- name: "08 · Synapse Workers + MAS (Docker, host network)"
  import_playbook: 08_workers.yml

- name: "09 · Element Web (Docker)"
  import_playbook: 09_element_web.yml

- name: "10 · HAProxy (native)"
  import_playbook: 10_haproxy.yml
